services:
  nginx-proxy:
    image: nginx:latest
    restart: always
    ports:
      - "70:70"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./proxy_conf.d:/etc/nginx/conf.d
      - ./logs:/var/log/nginx
    network_mode: "host"

#networks:
#  mcp-network:
#    driver: bridge